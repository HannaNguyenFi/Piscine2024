#include <stdio.h>
void  ft_div_mod(int a, int b, int *div, int *mod)
{
  *div = a / b;
  *mod = a % b;
}
/*
int  main(void)
{
  int a;
  int b;
  int mod;
  int div;

  a = 27;
  b = 12;

  printf("a is %d, b is %d, a, b);
  ft_div_mod(a, b, &div, &mod);
  printf("a is %d, b is %d, a/b is %d, and the remainder is %d\n", a, b, div, mod);
}
*/
